class Powerup:
    size(100, 100)
    photo = loadImage("defence.png")
    image(photo, 0, 0)size(100, 100)
    photo = loadImage("attack.png")
    image(photo, 0, 0)size(100, 100)
    photo = loadImage("health.png")
    image(photo, 0, 0)
    x = random
    y = random
    def__init__(self):
        fill(230)
        self.l = 0
        self.w = 0
        self.h = 0 
        self.x = 0
        self.y = 0
    #methods 
    def display(self):
    def attack(self):
    def health(self):
    def defence(self):
